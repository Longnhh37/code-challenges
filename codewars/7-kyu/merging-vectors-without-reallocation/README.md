<h2><a href=https://www.codewars.com/kata/617dcb2f242452004a77c653/train/rust target="_blank">Merging vectors without reallocation</a></h2><h3>7 kyu</h3><h1 id="help-needed">Help needed</h1><p>I want to implement a merging function for 2 vectors of integers without the need of copy, cloning or reallocation. The implementation seems right. So i need to think about the signature, especially the lifetimes. I tried to fix it. But it does not work. Can you help me to fix the problem?</p><h2 id="hint-1">Hint 1:</h2><p>Run the example code. The compiler will give you hints.</p><h2 id="hint-2">Hint 2:</h2><p>Rust introduces the concept of lifetime. More informations in the <a href="https://doc.rust-lang.org/book/ch10-03-lifetime-syntax.html" data-turbolinks="false" target="_blank">rustlang book</a>.</p><h2 id="hint-3">Hint 3:</h2><p>The result vector references to the values inside of the given vectors, so it does not allocates new space in heap memory. So the resulting vector needs to know, that the values cannot outlive the values of the given vectors.</p>